enclave {
    from "sgx_tstdc.edl" import *;

    trusted {
        /* define ECALLs here. */
		public void foo([out, size=len] char* buf, size_t len);
		public int trySeal([out, size=len] char* buf, size_t len,int  encrypt);
    };

    untrusted {
        /* define OCALLs here. */

    };
};
